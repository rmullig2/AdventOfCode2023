Notes
-----
Remove the extra row being added to the input
Transpose and push right for North tilt
Transpose back and push left for West tilt
Transpose again and push left for South tilt
Transpose back and push right for East tilt

Function to take list of strings and return np.array
----------------------------------------------------
Initialize empty list
For line in list of strings
	Append list(line) to empty list
Return np.array(empty list)

Modify transpose_and_tilt to add perform run 4 times representing each direction
    First direction North
        Using numpy flip the array on the vertical (0) axis
        Transpose the array
	Use the current code to tilt
        Call function to take the final strings and return an np.array
        Transpose the array
        Using numpy flip the array on the vertical (0) axis
    Next direction West
        Using numpy flip the array on the horizontal (1) axis
	Use the current code to tilt
        Call function to take the final strings and return an np.array
        Using numpy flip the array on the horizontal (1) axis
    Next direction South
        Transpose the array
	Use the current code to tilt
        Call function to take the final strings and return an np.array
        Transpose the array
    Next direction East
	Use the current code to tilt
        Call function to take the final strings and return an np.array
Return an np.array instead of a list of strings

Modify get_total_load to iterate over rows in array
	Convert each row to a string using	string = ''.join(row)
	Continue as written except add i+1 instead of i to compensate for the 0 index

# Problem states that this need to be run 4000000000 times
Change main function to run transport_and_tilt 1000 times
Save array after 1000 runs
Change main function to run continously
	After each 1000 runs
		Compare to saved array
			If different then overwrite saved array
			else terminate loop and get total load
